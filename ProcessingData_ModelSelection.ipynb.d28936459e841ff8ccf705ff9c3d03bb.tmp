fig, axList = plt.subplots(nrows=2, ncols=2)
axList = axList.flatten() # type: ignore
fig.set_size_inches(12, 10)
axList[-1].axis('off')

for ax,lab in zip(axList[:-1], coeff_labels):
    sns.heatmap(cm[lab], ax=ax, annot=True, fmt='d')
    ax.set(title=lab)
    sns.set(font_scale=2)
    
plt.tight_layout()